services:
  - type: web
    name: dqms-backend
    env: node
    buildCommand: npm install && npm run build:prod && npx prisma generate
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
      - key: DATABASE_URL
        sync: false
    healthCheckPath: /
